---
title: "projet Series chronologiques"
author: "Viny Presty NAKAVOUA"
date: "2024-11-06"
output: html_document
---


# 0. Objectif
On a les données d'une personne qui a réalisé une expérience
premiere phase avant regime et therapie : se pèse plusieurs fois par jour 
Deuxieme phase : se pèse une seule fois par jour durant le regime pendant deux semaines


# 1. importation des données

```{r, results='hide'}
library(readr)
data <- read_csv("poids.csv")
```


```{r}
head(data)
```


## pretraitement :

```{r}
data <- as.data.frame(data)
```

```{r}
data$Jour <- as.Date(data$Jour, format = "%d/%m/%y")
```

```{r}
head(data)
```


```{r}
plot(x=data$Jour, y=data$Poids)
```



## separer le dataset

Créer un dataset pour la premiere phase et un second pour la 2e phase.


# 2. Lissage par la méthode de regression locale

```{r}
# juste prendre le code dans le tutoriel, après s'être entendu sur le choix de la variable à confronter avec le poids : soit le jour ou l'heure ?
```










